#include <stdio.h>

int main(void)
{
   printf("如果把10用10進制來表示的話，就變成%d。\n", 10);
   printf("如果把10用8進制來表示的話，就變成%o。\n", 10);
   printf("如果把12用10進制來表示的話，就變成%o。\n", 12);
   printf("如果把10用16進制來表示的話，就變成%x。\n", 10);

   system("pause");
   return 0;
}
